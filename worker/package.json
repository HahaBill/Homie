{
  "name": "homie-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Homie backend — Cloudflare Worker (Hono) receiving/sending the Sendblue iMessage thread, backed by Supabase.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format=pretty",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.4.0"
  }
}
